:root {
  --bg-deep: #060912;
  --bg-base: #0a0f1e;
  --bg-elev: #111827;
  --bg-card: rgba(255, 255, 255, 0.04);
  --bg-card-hover: rgba(255, 255, 255, 0.07);
  --bg-input: rgba(255, 255, 255, 0.05);
  --border-subtle: rgba(255, 255, 255, 0.08);
  --border-strong: rgba(255, 255, 255, 0.16);
  --text-primary: #f8fafc;
  --text-secondary: #cbd5e1;
  --text-muted: #64748b;
  --primary: #0ea5e9;
  --primary-light: #38bdf8;
  --accent: #10b981;
  --accent-light: #34d399;
  --warn: #f59e0b;
  --warn-light: #fbbf24;
  --error: #ef4444;
  --grad-primary: linear-gradient(135deg, #0ea5e9 0%, #10b981 100%);
  --font-sans: "Inter", "Noto Sans TC", system-ui, sans-serif;
  --r-sm: 10px;
  --r-md: 14px;
  --r-lg: 20px;
  --r-xl: 28px;
  --r-full: 999px;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --dur: 0.4s;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
}

* { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

html, body {
  height: 100%;
  overflow: hidden;
  background: var(--bg-deep);
  color: var(--text-primary);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
  overscroll-behavior: none;
}

#root { height: 100%; width: 100%; }

button { font-family: inherit; cursor: pointer; border: none; background: none; color: inherit; }
input, textarea { font-family: inherit; -webkit-appearance: none; appearance: none; }
::selection { background: rgba(14, 165, 233, 0.4); color: #fff; }
::-webkit-scrollbar { width: 0; display: none; }

@keyframes fadeInUp { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes slideUp { from { transform: translateY(100%); } to { transform: translateY(0); } }
@keyframes spin { to { transform: rotate(360deg); } }
@keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }

.anim-up { animation: fadeInUp var(--dur) var(--ease-out) both; }
.anim-fade { animation: fadeIn var(--dur) var(--ease-out) both; }
.anim-slide-up { animation: slideUp 0.35s var(--ease-out) both; }
===APP.CSS===
/* ============================================================
   译境 LinguaVerse — Design System
   Light theme · cobalt accent · paper surface · premium feel
   Inspired by DeepL iOS, Apple Translate, Notion, Linear
   ============================================================ */

/* Variables scoped to .app-shell so they reliably override
   the dark :root tokens defined in global.css. */
.app-shell {
  /* Surfaces */
  --bg-page: #FAFAF7;          /* warm off-white / paper */
  --bg-card: #FFFFFF;          /* pure white surface */
  --bg-input: #F9FAFB;         /* subtle input fill */
  --bg-tint: #EFF3FF;          /* light blue tint (active) */
  --bg-hover: #F3F4F6;         /* neutral hover */

  /* Text */
  --text-primary: #1A1D29;     /* near-black */
  --text-secondary: #6B7280;   /* gray */
  --text-tertiary: #9CA3AF;    /* light gray */

  /* Borders */
  --border: #E5E7EB;
  --border-strong: #D1D5DB;

  /* Brand — cobalt blue */
  --primary: #3B5BDB;
  --primary-light: #5C7CFA;
  --primary-dark: #2B4FCB;
  --primary-tint: #EFF3FF;
  --primary-tint-strong: #DBE4FF;
  --primary-border: #BAC8FF;

  /* Success — green */
  --accent: #2F9E44;
  --accent-light: #37B24D;
  --accent-tint: #EBFBEE;
  --accent-border: #B2F2BB;

  /* Status */
  --warn: #E67700;
  --warn-tint: #FFF4E6;
  --error: #E03131;
  --error-tint: #FFF5F5;

  /* Subtle brand gradient */
  --grad-primary: linear-gradient(135deg, #3B5BDB 0%, #5C7CFA 100%);

  /* Radii */
  --r-xs: 6px;
  --r-sm: 8px;
  --r-md: 12px;   /* inputs */
  --r-lg: 16px;   /* cards */
  --r-xl: 24px;   /* sheets */
  --r-full: 999px;

  /* Shadows — soft, layered, never neon */
  --shadow-xs: 0 1px 2px rgba(16, 24, 40, 0.04);
  --shadow-sm: 0 1px 2px rgba(16, 24, 40, 0.04), 0 4px 12px rgba(16, 24, 40, 0.04);
  --shadow-md: 0 2px 4px rgba(16, 24, 40, 0.05), 0 8px 20px rgba(16, 24, 40, 0.06);
  --shadow-lg: 0 4px 12px rgba(16, 24, 40, 0.06), 0 20px 40px rgba(16, 24, 40, 0.08);
  --shadow-focus: 0 0 0 3px rgba(59, 91, 219, 0.18);
  --shadow-primary: 0 1px 2px rgba(59, 91, 219, 0.18), 0 6px 16px rgba(59, 91, 219, 0.22);

  /* Motion */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --dur: 0.28s;

  /* Safe areas (mirror global.css) */
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);

  /* Typography */
  --font-sans: "Inter", "Noto Sans TC", -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
}

/* ============================================================
   App shell
   ============================================================ */
.app-shell {
  height: 100%;
  width: 100%;
  max-width: 480px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  position: relative;
  overflow: hidden;
  background: var(--bg-page);
  color: var(--text-primary);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  letter-spacing: -0.005em;
}

/* Subtle selection colour inside the app */
.app-shell ::selection {
  background: rgba(59, 91, 219, 0.18);
  color: inherit;
}

/* ============================================================
   Header — minimal: brand name + plan badge
   ============================================================ */
.app-header {
  flex-shrink: 0;
  padding: calc(var(--safe-top) + 14px) 20px 10px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-page);
  z-index: 10;
}

.header-brand {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}
.header-brand b {
  font-size: 17px;
  font-weight: 700;
  letter-spacing: -0.015em;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.header-brand span {
  font-size: 11px;
  font-weight: 500;
  color: var(--text-tertiary);
  letter-spacing: 0.02em;
  margin-top: 1px;
  display: block;
  line-height: 1.1;
}

.header-plan-badge {
  font-size: 10.5px;
  font-weight: 700;
  padding: 5px 11px;
  border-radius: var(--r-full);
  letter-spacing: 0.08em;
  background: var(--primary-tint);
  border: 1px solid var(--primary-border);
  color: var(--primary);
  transition: transform 0.18s var(--ease-out), background 0.18s var(--ease-out);
  flex-shrink: 0;
}
.header-plan-badge:active { transform: scale(0.94); }

.app-content {
  flex: 1;
  overflow-y: auto;
  overflow-x: hidden;
  -webkit-overflow-scrolling: touch;
}
.app-content::-webkit-scrollbar { display: none; }

.page {
  min-height: 100%;
  padding: 4px 18px calc(var(--safe-bottom) + 104px);
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.page-title {
  font-size: 26px;
  font-weight: 700;
  letter-spacing: -0.025em;
  color: var(--text-primary);
  padding: 8px 2px 4px;
}

/* ============================================================
   Bottom navigation — solid white, top border, blue active
   ============================================================ */
.bottom-nav {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 8px 12px calc(var(--safe-bottom) + 8px);
  background: var(--bg-card);
  border-top: 1px solid var(--border);
  z-index: 20;
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
  padding: 6px 18px;
  border-radius: var(--r-md);
  color: var(--text-tertiary);
  transition: color 0.2s var(--ease-out), transform 0.18s var(--ease-out);
  position: relative;
}
.nav-item:active { transform: scale(0.92); }
.nav-item.active { color: var(--primary); }
.nav-item svg {
  width: 22px;
  height: 22px;
  transition: transform 0.28s var(--ease-spring);
}
.nav-item.active svg { transform: translateY(-1px); }
.nav-item span {
  font-size: 10.5px;
  font-weight: 600;
  letter-spacing: 0.02em;
}
.nav-item.active::before {
  content: "";
  position: absolute;
  top: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 20px;
  height: 2.5px;
  border-radius: var(--r-full);
  background: var(--primary);
}

/* ============================================================
   Translate page
   ============================================================ */
.translate-page { gap: 14px; }

/* Language bar — single white card with 3 segments */
.lang-bar {
  display: flex;
  align-items: stretch;
  gap: 4px;
  padding: 6px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  box-shadow: var(--shadow-sm);
}
.lang-select {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 2px;
  padding: 9px 12px;
  border-radius: var(--r-md);
  background: transparent;
  transition: background 0.18s var(--ease-out);
  text-align: left;
  min-width: 0;
}
.lang-select:active { background: var(--bg-hover); }
.lang-select-meta {
  font-size: 10px;
  font-weight: 600;
  color: var(--text-tertiary);
  letter-spacing: 0.1em;
  text-transform: uppercase;
}
.lang-select-current {
  display: flex;
  align-items: center;
  gap: 7px;
  min-width: 0;
}
.lang-flag { font-size: 20px; line-height: 1; flex-shrink: 0; }
.lang-name {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.lang-swap {
  align-self: center;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--primary-tint);
  color: var(--primary);
  flex-shrink: 0;
  transition: transform 0.4s var(--ease-spring), background 0.18s var(--ease-out);
}
.lang-swap:active { transform: rotate(180deg) scale(0.9); background: var(--primary-tint-strong); }
.lang-swap svg { width: 16px; height: 16px; }

/* Input card */
.input-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  box-shadow: var(--shadow-sm);
  transition: border-color 0.18s var(--ease-out), box-shadow 0.18s var(--ease-out);
}
.input-card:focus-within {
  border-color: var(--primary-border);
  box-shadow: var(--shadow-sm), var(--shadow-focus);
}
.translate-textarea {
  width: 100%;
  min-height: 140px;
  background: transparent;
  border: none;
  outline: none;
  resize: none;
  color: var(--text-primary);
  font-size: 16px;
  line-height: 1.55;
  font-family: inherit;
}
.translate-textarea::placeholder { color: var(--text-tertiary); }
.input-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 10px;
  border-top: 1px solid var(--border);
}
.char-count {
  font-size: 12px;
  color: var(--text-tertiary);
  font-variant-numeric: tabular-nums;
  font-weight: 500;
}
.char-count.warn { color: var(--warn); font-weight: 600; }
.input-actions { display: flex; gap: 4px; }
.icon-btn {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-input);
  color: var(--text-secondary);
  transition: background 0.18s var(--ease-out), transform 0.18s var(--ease-out);
}
.icon-btn:active:not(:disabled) { background: var(--bg-hover); transform: scale(0.9); }
.icon-btn:disabled { opacity: 0.35; }
.icon-btn svg { width: 16px; height: 16px; }

/* ============================================================
   VoiceInput — premium industrial-minimalist mic button
   ============================================================
   States: idle (hairline border + hover glow), recording (red breathing
   pulse + live timer), transcribing (blue spinner + label).
   ============================================================ */

.voice-input-wrap {
  position: relative;
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.voice-input-btn {
  /* Sizing */
  --btn-size: 38px;
  width: var(--btn-size);
  height: var(--btn-size);
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  position: relative;
  isolation: isolate; /* contain the pulse layer */

  /* Default appearance — hairline border, transparent fill */
  background: transparent;
  color: var(--text-secondary);
  border: 1px solid var(--border-strong, #D1D5DB);
  cursor: pointer;
  transition: color 0.28s var(--ease-out, ease),
              border-color 0.28s var(--ease-out, ease),
              background 0.28s var(--ease-out, ease),
              box-shadow 0.32s var(--ease-out, ease),
              transform 0.18s var(--ease-out, ease);
}

/* Idle hover — soft cyan micro-glow on the border (微光) */
.voice-input-btn.state-idle:hover:not(:disabled) {
  color: var(--primary, #3B5BDB);
  border-color: var(--primary, #3B5BDB);
  box-shadow:
    0 0 0 4px rgba(59, 91, 219, 0.10),
    0 0 14px rgba(59, 91, 219, 0.18);
}

/* Pressed */
.voice-input-btn:active:not(:disabled) {
  transform: scale(0.94);
}

/* Disabled */
.voice-input-btn:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

/* Icon sizing */
.voice-input-icon {
  width: 17px;
  height: 17px;
  position: relative;
  z-index: 2;
  transition: opacity 0.2s ease;
}

/* ----- Recording state ----- */
.voice-input-btn.state-recording {
  background: linear-gradient(135deg, #EF4444 0%, #DC2626 100%);
  border-color: transparent;
  color: #fff;
  box-shadow:
    0 6px 16px rgba(220, 38, 38, 0.35),
    0 0 0 0 rgba(239, 68, 68, 0.5);
  animation: voice-breathe 1.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

/* The breathing pulse layer (a separate ::before-style span inside the
   button so we can animate it independently of the icon) */
.voice-input-pulse {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s ease;
}
.voice-input-btn.state-recording .voice-input-pulse {
  opacity: 1;
  background: radial-gradient(circle, rgba(255,255,255,0.0) 60%, rgba(239,68,68,0.5) 100%);
  animation: voice-ring 1.8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes voice-breathe {
  0%, 100% {
    transform: scale(1);
    box-shadow:
      0 6px 16px rgba(220, 38, 38, 0.35),
      0 0 0 0 rgba(239, 68, 68, 0.45);
  }
  50% {
    transform: scale(1.06);
    box-shadow:
      0 8px 22px rgba(220, 38, 38, 0.45),
      0 0 0 8px rgba(239, 68, 68, 0);
  }
}

@keyframes voice-ring {
  0%   { transform: scale(1);   opacity: 0.7; }
  100% { transform: scale(1.8); opacity: 0;   }
}

/* Live timer shown next to the icon while recording */
.voice-input-timer {
  position: absolute;
  bottom: -22px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 10.5px;
  font-weight: 600;
  letter-spacing: 0.04em;
  color: #DC2626;
  background: #fff;
  border: 1px solid #FCA5A5;
  border-radius: 999px;
  padding: 2px 8px;
  white-space: nowrap;
  box-shadow: 0 2px 6px rgba(220, 38, 38, 0.18);
}

/* ----- Transcribing state ----- */
.voice-input-btn.state-transcribing {
  background: var(--primary-tint, #EFF3FF);
  border-color: var(--primary, #3B5BDB);
  color: var(--primary, #3B5BDB);
  cursor: wait;
}

.voice-input-spinner {
  animation: voice-spin 0.9s linear infinite;
}

@keyframes voice-spin {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}

/* Label shown beside the spinner */
.voice-input-label {
  position: absolute;
  bottom: -22px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 10.5px;
  font-weight: 600;
  letter-spacing: 0.04em;
  color: var(--primary, #3B5BDB);
  background: var(--primary-tint, #EFF3FF);
  border: 1px solid var(--primary-border, #C7D2FE);
  border-radius: 999px;
  padding: 2px 8px;
  white-space: nowrap;
}

/* ----- Cancel button (shown only while recording) ----- */
.voice-input-cancel {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: #fff;
  color: #6B7280;
  border: 1px solid var(--border-strong, #D1D5DB);
  cursor: pointer;
  transition: background 0.18s ease, color 0.18s ease, transform 0.18s ease;
}
.voice-input-cancel:hover {
  background: #F9FAFB;
  color: #DC2626;
  border-color: #FCA5A5;
}
.voice-input-cancel:active { transform: scale(0.9); }
.voice-input-cancel svg { width: 14px; height: 14px; }

/* Reduced motion — respect user preference */
@media (prefers-reduced-motion: reduce) {
  .voice-input-btn.state-recording,
  .voice-input-btn.state-recording .voice-input-pulse,
  .voice-input-spinner {
    animation: none;
  }
  .voice-input-btn.state-recording {
    background: #DC2626;
    box-shadow: 0 4px 12px rgba(220, 38, 38, 0.3);
  }
}

/* Translate button — solid cobalt */
.translate-btn {
  width: 100%;
  padding: 15px;
  border-radius: var(--r-md);
  background: var(--primary);
  color: #FFFFFF;
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.01em;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  box-shadow: var(--shadow-primary);
  transition: transform 0.18s var(--ease-out), background 0.18s var(--ease-out), opacity 0.18s var(--ease-out);
}
.translate-btn:active:not(:disabled) { transform: scale(0.98); background: var(--primary-dark); }
.translate-btn:disabled { opacity: 0.5; box-shadow: none; }
.translate-btn svg { width: 18px; height: 18px; }
.translate-btn.loading { pointer-events: none; }

.btn-dots { display: inline-flex; gap: 4px; margin-left: 2px; }
.btn-dots span {
  width: 5px; height: 5px; border-radius: 50%; background: #FFFFFF;
  animation: btnDot 1.2s var(--ease-out) infinite;
}
.btn-dots span:nth-child(2) { animation-delay: 0.18s; }
.btn-dots span:nth-child(3) { animation-delay: 0.36s; }
@keyframes btnDot {
  0%, 80%, 100% { opacity: 0.3; transform: translateY(0); }
  40% { opacity: 1; transform: translateY(-2px); }
}

.quota-hint {
  font-size: 11.5px;
  color: var(--text-tertiary);
  text-align: center;
  padding: 2px 4px 0;
  font-weight: 500;
}
.quota-hint b { color: var(--primary); font-weight: 700; }

/* Result card */
.result-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  box-shadow: var(--shadow-sm);
  position: relative;
  overflow: hidden;
}
.result-card::before {
  content: "";
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 3px;
  background: var(--grad-primary);
}
.result-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
}
.result-label {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  font-weight: 700;
  color: var(--primary);
  text-transform: uppercase;
  letter-spacing: 0.08em;
}
.result-label svg { width: 14px; height: 14px; }

/* Copy button — visually prominent */
.copy-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 7px 13px;
  border-radius: var(--r-full);
  background: var(--primary-tint);
  border: 1px solid var(--primary-border);
  color: var(--primary);
  font-size: 12px;
  font-weight: 600;
  transition: transform 0.18s var(--ease-out), background 0.18s var(--ease-out), color 0.18s var(--ease-out), border-color 0.18s var(--ease-out);
  flex-shrink: 0;
}
.copy-btn:active { transform: scale(0.94); }
.copy-btn svg { width: 14px; height: 14px; }
.copy-btn.copied {
  background: var(--accent-tint);
  border-color: var(--accent-border);
  color: var(--accent);
}

.result-text {
  font-size: 17px;
  line-height: 1.6;
  color: var(--text-primary);
  font-weight: 500;
  word-break: break-word;
  white-space: pre-wrap;
}
.result-meta {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  padding-top: 10px;
  border-top: 1px solid var(--border);
  min-height: 18px;
}
.result-saved {
  font-size: 11px;
  color: var(--accent);
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}
.result-saved svg { width: 12px; height: 12px; }

/* Loading skeleton inside result */
.result-skeleton { display: flex; flex-direction: column; gap: 10px; padding: 4px 0; }
.sk-line { height: 14px; border-radius: var(--r-sm); }
.sk-line.shimmer {
  background: linear-gradient(90deg, #F3F4F6 0%, #E5E7EB 50%, #F3F4F6 100%);
  background-size: 200% 100%;
  animation: shimmer 1.4s infinite;
}

/* Streaming cursor — shown while the translation streams in */
.streaming-cursor {
  display: inline-block;
  width: 2px;
  height: 1em;
  margin-left: 4px;
  background: var(--accent);
  vertical-align: text-bottom;
  animation: blink-cursor 1s steps(2) infinite;
}
@keyframes blink-cursor {
  0%, 50% { opacity: 1; }
  50.01%, 100% { opacity: 0; }
}

/* ============================================================
   History page
   ============================================================ */
.history-filter { display: flex; gap: 8px; padding: 0 2px; }
.filter-chip {
  padding: 7px 14px;
  border-radius: var(--r-full);
  background: var(--bg-card);
  border: 1px solid var(--border);
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  transition: all 0.18s var(--ease-out);
  display: inline-flex;
  align-items: center;
  gap: 5px;
}
.filter-chip:active { transform: scale(0.95); }
.filter-chip.active {
  background: var(--primary-tint);
  border-color: var(--primary-border);
  color: var(--primary);
}

.history-list { display: flex; flex-direction: column; gap: 10px; }
.history-item {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: 14px 15px;
  display: flex;
  flex-direction: column;
  gap: 7px;
  box-shadow: var(--shadow-xs);
  transition: transform 0.18s var(--ease-out), box-shadow 0.18s var(--ease-out);
}
.history-item:active { transform: scale(0.985); box-shadow: var(--shadow-sm); }
.history-item-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}
.history-lang-pair {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 11.5px;
  font-weight: 500;
  color: var(--text-secondary);
  min-width: 0;
  flex-wrap: nowrap;
}
.history-lang-pair .lang-flag { font-size: 15px; }
.history-lang-pair svg { width: 11px; height: 11px; opacity: 0.55; flex-shrink: 0; }
.history-actions { display: flex; gap: 2px; flex-shrink: 0; }
.history-fav-btn {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-tertiary);
  transition: color 0.18s var(--ease-out), background 0.18s var(--ease-out), transform 0.18s var(--ease-out);
}
.history-fav-btn:active { transform: scale(0.85); background: var(--bg-hover); }
.history-fav-btn.active { color: #F59E0B; }
.history-fav-btn svg { width: 16px; height: 16px; }
.history-source {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.45;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}
.history-target {
  font-size: 14.5px;
  color: var(--text-primary);
  font-weight: 500;
  line-height: 1.5;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}
.history-time {
  font-size: 11px;
  color: var(--text-tertiary);
  font-weight: 500;
  padding-top: 2px;
}

/* Empty state */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  padding: 56px 20px;
  text-align: center;
}
.empty-icon {
  width: 72px;
  height: 72px;
  border-radius: 50%;
  background: var(--bg-card);
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-tertiary);
}
.empty-icon svg { width: 30px; height: 30px; }
.empty-title { font-size: 15px; font-weight: 700; color: var(--text-primary); }
.empty-desc { font-size: 13px; color: var(--text-secondary); max-width: 240px; line-height: 1.55; }

/* ============================================================
   Settings page
   ============================================================ */
.settings-page { gap: 14px; }

.usage-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: 18px;
  display: flex;
  flex-direction: column;
  gap: 14px;
  box-shadow: var(--shadow-sm);
  position: relative;
  overflow: hidden;
}
.usage-head { display: flex; align-items: center; justify-content: space-between; }
.usage-plan {
  font-size: 17px;
  font-weight: 700;
  letter-spacing: -0.01em;
  color: var(--text-primary);
}
.usage-bar {
  height: 8px;
  border-radius: var(--r-full);
  background: var(--bg-hover);
  overflow: hidden;
}
.usage-bar-fill {
  height: 100%;
  border-radius: var(--r-full);
  background: var(--grad-primary);
  transition: width 0.6s var(--ease-out);
}
.usage-stats {
  display: flex;
  justify-content: space-between;
  font-size: 12.5px;
}
.usage-used { color: var(--text-primary); font-weight: 700; font-variant-numeric: tabular-nums; }
.usage-total { color: var(--text-tertiary); font-weight: 500; }

.upgrade-btn {
  width: 100%;
  padding: 13px;
  border-radius: var(--r-md);
  background: var(--primary);
  color: #FFFFFF;
  font-size: 14.5px;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  box-shadow: var(--shadow-primary);
  transition: transform 0.18s var(--ease-out), background 0.18s var(--ease-out);
}
.upgrade-btn:active { transform: scale(0.98); background: var(--primary-dark); }
.upgrade-btn svg { width: 17px; height: 17px; }

.settings-section { display: flex; flex-direction: column; gap: 8px; }
.section-label {
  font-size: 11px;
  font-weight: 700;
  color: var(--text-tertiary);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 4px 4px 0;
}
.settings-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  overflow: hidden;
  box-shadow: var(--shadow-xs);
}
.settings-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 13px 15px;
  border-bottom: 1px solid var(--border);
  transition: background 0.18s var(--ease-out);
}
.settings-row:last-child { border-bottom: none; }
.settings-row:active { background: var(--bg-hover); }
.row-label { display: flex; align-items: center; gap: 12px; min-width: 0; }
.row-icon {
  width: 32px;
  height: 32px;
  border-radius: var(--r-sm);
  background: var(--primary-tint);
  color: var(--primary);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.row-icon svg { width: 17px; height: 17px; }
.row-text { display: flex; flex-direction: column; gap: 1px; min-width: 0; }
.row-title { font-size: 14px; font-weight: 600; color: var(--text-primary); }
.row-desc { font-size: 11.5px; color: var(--text-secondary); line-height: 1.4; }
.row-value {
  font-size: 13px;
  color: var(--text-secondary);
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 6px;
  flex-shrink: 0;
}
.row-value .lang-flag { font-size: 16px; }
.chevron { color: var(--text-tertiary); display: inline-flex; }
.chevron svg { width: 16px; height: 16px; }

.about-foot {
  text-align: center;
  font-size: 11.5px;
  color: var(--text-tertiary);
  padding: 14px 0 4px;
  line-height: 1.6;
}
.about-foot b { color: var(--text-secondary); font-weight: 600; }

/* ============================================================
   Pricing page
   ============================================================ */
.pricing-page { gap: 14px; }
.pricing-header { text-align: center; padding: 14px 0 4px; }
.pricing-title {
  font-size: 24px;
  font-weight: 700;
  letter-spacing: -0.025em;
  margin-bottom: 7px;
  color: var(--text-primary);
}
.pricing-subtitle { font-size: 13.5px; color: var(--text-secondary); line-height: 1.5; }
.pricing-cards { display: flex; flex-direction: column; gap: 12px; }
.pricing-plan {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 14px;
  box-shadow: var(--shadow-sm);
  transition: transform 0.22s var(--ease-out), border-color 0.22s var(--ease-out), box-shadow 0.22s var(--ease-out);
}
.pricing-plan.featured {
  border-color: var(--primary-border);
  box-shadow: var(--shadow-md);
  background: linear-gradient(180deg, rgba(59, 91, 219, 0.04) 0%, var(--bg-card) 60%);
  position: relative;
}
.pricing-plan-head { display: flex; align-items: center; justify-content: space-between; }
.pricing-plan-name { font-size: 17px; font-weight: 700; letter-spacing: -0.01em; color: var(--text-primary); }
.pricing-plan-badge {
  font-size: 10.5px;
  font-weight: 700;
  padding: 4px 10px;
  border-radius: var(--r-full);
  letter-spacing: 0.04em;
  background: var(--primary-tint);
  color: var(--primary);
}
.pricing-plan-price { display: flex; align-items: baseline; gap: 4px; }
.pricing-plan-amount { font-size: 34px; font-weight: 700; letter-spacing: -0.025em; color: var(--text-primary); }
.pricing-plan-period { font-size: 13px; color: var(--text-tertiary); font-weight: 500; }
.pricing-plan-features { list-style: none; display: flex; flex-direction: column; gap: 9px; }
.pricing-plan-features li {
  display: flex;
  align-items: flex-start;
  gap: 9px;
  font-size: 13.5px;
  color: var(--text-secondary);
  line-height: 1.45;
}
.pricing-plan-features li svg {
  width: 16px;
  height: 16px;
  color: var(--accent);
  flex-shrink: 0;
  margin-top: 2px;
}
.pricing-plan-cta {
  width: 100%;
  padding: 13px;
  border-radius: var(--r-md);
  font-size: 14.5px;
  font-weight: 600;
  transition: transform 0.18s var(--ease-out), background 0.18s var(--ease-out);
}
.pricing-plan-cta:active:not(:disabled) { transform: scale(0.98); }
.pricing-plan-cta.primary {
  background: var(--primary);
  color: #FFFFFF;
  box-shadow: var(--shadow-primary);
}
.pricing-plan-cta.primary:active { background: var(--primary-dark); }
.pricing-plan-cta.secondary {
  background: var(--bg-card);
  border: 1px solid var(--border-strong);
  color: var(--text-primary);
}
.pricing-plan-cta.secondary:active { background: var(--bg-hover); }
.pricing-plan-cta:disabled { opacity: 0.55; }

/* ============================================================
   Paywall — bottom sheet, solid white
   ============================================================ */
.paywall-overlay {
  position: fixed;
  inset: 0;
  background: rgba(16, 24, 40, 0.42);
  z-index: 150;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  animation: fadeIn 0.3s var(--ease-out) both;
}
.paywall {
  width: 100%;
  max-width: 480px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-bottom: none;
  border-radius: var(--r-xl) var(--r-xl) 0 0;
  padding: 24px 22px calc(var(--safe-bottom) + 22px);
  display: flex;
  flex-direction: column;
  gap: 18px;
  animation: slideUp 0.36s var(--ease-out) both;
  box-shadow: var(--shadow-lg);
}
.paywall-icon {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background: var(--primary-tint);
  color: var(--primary);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto;
}
.paywall-icon svg { width: 28px; height: 28px; }
.paywall-title {
  text-align: center;
  font-size: 21px;
  font-weight: 700;
  letter-spacing: -0.015em;
  color: var(--text-primary);
}
.paywall-desc {
  text-align: center;
  font-size: 13.5px;
  color: var(--text-secondary);
  line-height: 1.55;
}
.paywall-features { display: flex; flex-direction: column; gap: 11px; padding: 4px 0; }
.paywall-feature {
  display: flex;
  align-items: center;
  gap: 11px;
  font-size: 13.5px;
  color: var(--text-primary);
}
.paywall-feature svg {
  width: 18px;
  height: 18px;
  color: var(--accent);
  flex-shrink: 0;
}
.paywall-close {
  text-align: center;
  font-size: 13.5px;
  color: var(--text-secondary);
  padding: 6px;
  font-weight: 500;
}

/* ============================================================
   Bottom sheet — solid white, 24px top radius
   ============================================================ */
.sheet-overlay {
  position: fixed;
  inset: 0;
  background: rgba(16, 24, 40, 0.42);
  z-index: 100;
  animation: fadeIn 0.25s var(--ease-out) both;
}
.sheet {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  margin: 0 auto;
  width: 100%;
  max-width: 480px;
  max-height: 72vh;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-bottom: none;
  border-radius: var(--r-xl) var(--r-xl) 0 0;
  z-index: 101;
  display: flex;
  flex-direction: column;
  animation: slideUp 0.34s var(--ease-out) both;
  padding-bottom: var(--safe-bottom);
  box-shadow: var(--shadow-lg);
}
.sheet-handle {
  width: 36px;
  height: 4px;
  border-radius: var(--r-full);
  background: var(--border-strong);
  margin: 10px auto 0;
  flex-shrink: 0;
}
.sheet-header {
  padding: 14px 20px 6px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.sheet-title {
  font-size: 17px;
  font-weight: 700;
  letter-spacing: -0.015em;
  color: var(--text-primary);
}
.sheet-close {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-input);
  color: var(--text-secondary);
  transition: background 0.18s var(--ease-out), transform 0.18s var(--ease-out);
}
.sheet-close:active { transform: scale(0.9); background: var(--bg-hover); }
.sheet-close svg { width: 16px; height: 16px; }
.sheet-list {
  flex: 1;
  overflow-y: auto;
  padding: 6px 10px 18px;
  -webkit-overflow-scrolling: touch;
}
.sheet-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 11px 14px;
  border-radius: var(--r-md);
  transition: background 0.18s var(--ease-out);
  text-align: left;
  width: 100%;
}
.sheet-item:active { background: var(--bg-hover); }
.sheet-item.selected { background: var(--primary-tint); }
.sheet-item-flag { font-size: 22px; width: 30px; text-align: center; flex-shrink: 0; }
.sheet-item-text { flex: 1; display: flex; flex-direction: column; gap: 2px; min-width: 0; }
.sheet-item-name { font-size: 14.5px; font-weight: 600; color: var(--text-primary); }
.sheet-item.selected .sheet-item-name { color: var(--primary); }
.sheet-item-native { font-size: 12.5px; color: var(--text-secondary); }
.sheet-item-check { color: var(--primary); flex-shrink: 0; }
.sheet-item-check svg { width: 18px; height: 18px; }

/* ============================================================
   Toast — bottom-center pill, sits just above the bottom nav
   ============================================================ */
.toast {
  position: fixed;
  bottom: calc(72px + var(--safe-bottom));
  left: 50%;
  transform: translateX(-50%);
  background: #1A1D29;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: var(--r-full);
  padding: 12px 22px;
  font-size: 13.5px;
  font-weight: 600;
  color: #FFFFFF;
  z-index: 200;
  box-shadow: 0 8px 28px rgba(0, 0, 0, 0.35), 0 0 0 1px rgba(255, 255, 255, 0.02);
  animation: toastIn 0.32s var(--ease-out) both;
  white-space: nowrap;
  max-width: 90vw;
  overflow: hidden;
  text-overflow: ellipsis;
  letter-spacing: 0.01em;
}

@keyframes toastIn {
  from { opacity: 0; transform: translate(-50%, 12px); }
  to   { opacity: 1; transform: translate(-50%, 0); }
}

/* ============================================================
   Skeleton shimmer (shared)
   ============================================================ */
.skeleton {
  background: linear-gradient(90deg, #F3F4F6 0%, #E5E7EB 50%, #F3F4F6 100%);
  background-size: 200% 100%;
  animation: shimmer 1.4s infinite;
  border-radius: var(--r-sm);
}

/* ============================================================
   STT model management (Settings page)
   ============================================================ */
.stt-progress {
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.stt-progress-bar {
  width: 100%;
  height: 6px;
  background: var(--bg-input, rgba(255,255,255,0.05));
  border-radius: var(--r-full);
  overflow: hidden;
}
.stt-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--primary) 0%, var(--primary-light) 100%);
  border-radius: var(--r-full);
  transition: width 0.3s var(--ease-out);
}
.stt-progress-text {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-secondary);
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.02em;
}
.stt-actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}
.stt-btn {
  flex: 1;
  min-width: 120px;
  padding: 11px 16px;
  border-radius: var(--r-md);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  font-size: 14px;
  font-weight: 600;
  background: var(--bg-input, rgba(255,255,255,0.05));
  color: var(--text-primary);
  border: 1px solid var(--border-strong, rgba(255,255,255,0.16));
  transition: background 0.18s var(--ease-out), transform 0.18s var(--ease-out);
}
.stt-btn:active:not(:disabled) { transform: scale(0.97); }
.stt-btn:disabled { opacity: 0.5; cursor: not-allowed; }
.stt-btn svg { width: 16px; height: 16px; }
.stt-btn-primary {
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
  color: #FFFFFF;
  border-color: transparent;
  box-shadow: 0 4px 12px rgba(14, 165, 233, 0.25);
}
.stt-btn-danger {
  color: #EF4444;
  border-color: rgba(239, 68, 68, 0.3);
}

/* ============================================================
   Ad Overlay
   ============================================================ */
.ad-overlay {
  position: fixed;
  inset: 0;
  z-index: 500;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  animation: fadeIn 0.25s ease both;
}

.ad-card {
  width: 100%;
  max-width: 400px;
  background: var(--bg-elev, #111827);
  border-radius: var(--r-lg);
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
}

.ad-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 16px;
  background: rgba(255, 255, 255, 0.04);
  border-bottom: 1px solid var(--border-subtle);
}

.ad-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  text-transform: uppercase;
}

.ad-countdown {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-secondary);
  font-variant-numeric: tabular-nums;
}

.ad-close {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: transparent;
  color: var(--text-secondary);
  font-size: 20px;
  line-height: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  border: none;
}
.ad-close:hover { background: rgba(255,255,255,0.08); color: var(--text-primary); }

.ad-body {
  padding: 40px 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
}

.ad-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  color: var(--text-secondary);
}
.ad-placeholder p { font-size: 15px; font-weight: 600; margin: 0; }
.ad-placeholder .ad-subtitle { font-size: 13px; font-weight: 400; color: var(--text-muted); }

.ad-footer { padding: 16px; border-top: 1px solid var(--border-subtle); }

.ad-continue-btn {
  width: 100%;
  padding: 14px;
  border-radius: var(--r-md);
  background: var(--primary);
  color: #FFFFFF;
  font-size: 15px;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: transform 0.18s var(--ease-out), opacity 0.18s var(--ease-out);
}
.ad-continue-btn:active:not(:disabled) { transform: scale(0.97); }
.ad-continue-btn:disabled { opacity: 0.4; cursor: not-allowed; }

/* Header quota badge (replaces plan badge) */
.header-quota-badge {
  font-size: 12px;
  font-weight: 600;
  color: var(--text-secondary);
  background: var(--bg-card);
  border: 1px solid var(--border-subtle);
  border-radius: var(--r-full);
  padding: 4px 12px;
  letter-spacing: 0.02em;
}